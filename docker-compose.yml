version: '3.8'

services:
  chatbot:
    build: .
    container_name: gemini_chatbot
    env_file:
      - .env
    stdin_open: true
    tty: true
    ports:
      - "8501:8501"
    volumes:
      - ./chroma_data:/app/chroma_chat_memory
